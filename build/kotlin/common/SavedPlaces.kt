// AUTO-GENERATED by scripts/build.mjs from src/constants/saved-places.json. Do not edit.
package dog.yomp.contracts

// saved_places write-model — field limits + doc id (deployed firestore.rules `saved_places`).
// Personal bookmark data (a user's own saved list), kept separate from the community write-path
// (Community.kt). Each *_MIN/_MAX caps the FIELD named in its comment, NOT the composite doc id.
object SavedPlaces {
    const val SAVED_PLACES_COLLECTION: String = "saved_places"

    const val SAVED_PLACE_ID_MIN: Int = 1 // placeId field, min length
    const val SAVED_PLACE_ID_MAX: Int = 200 // placeId field, max length
    const val SAVED_PLACE_NAME_MIN: Int = 1 // placeName field, min length
    const val SAVED_PLACE_NAME_MAX: Int = 200 // placeName field, max length
    const val SAVED_PLACE_TYPE_MAX: Int = 100 // placeType field, max length
    const val SAVED_PLACE_PHOTO_NAME_MAX: Int = 500 // photoName field, max length
    const val SAVED_PLACE_LAT_MIN: Int = -90 // placeLat field
    const val SAVED_PLACE_LAT_MAX: Int = 90 // placeLat field
    const val SAVED_PLACE_LNG_MIN: Int = -180 // placeLng field
    const val SAVED_PLACE_LNG_MAX: Int = 180 // placeLng field
    const val SAVED_PLACE_RATING_MIN: Int = 1 // rating field
    const val SAVED_PLACE_RATING_MAX: Int = 5 // rating field

    /**
     * Firestore doc id for a saved_places write: "${uid}_${safeId(placeId)}".
     * Reuses Community.safeId (same package + common source set) — safeId is NOT duplicated here.
     * The deployed rule enforces saveId == uid + '_' + placeId where the stored placeId FIELD is
     * the safeId form, so the doc id and the field share the sanitised value.
     */
    fun savedPlaceDocId(uid: String, placeId: String): String = "${uid}_${Community.safeId(placeId)}"
}
